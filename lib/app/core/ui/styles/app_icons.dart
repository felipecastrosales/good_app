import 'package:flutter/material.dart';
import 'package:good_app/app/core/core.dart';

class AppIcons {
  static const drawer = Icon(
    Icons.menu,
    color: AppColors.primary,
  );

  static const info = Icon(
    Icons.info_outline,
    color: AppColors.background,
    size: 28,
  );

  static const error = Icon(
    Icons.error,
    color: AppColors.background,
    size: 28,
  );

  static const check = Icon(
    Icons.check_circle,
    color: AppColors.background,
    size: 28,
  );

  static const cache = Icon(
    Icons.cached,
    color: AppColors.background,
    size: 28,
  );

  static const user = Icon(
    Icons.person_rounded,
    size: 80,
    color: AppColors.dark,
  );

  static const exit = Icon(
    Icons.exit_to_app_outlined,
    color: AppColors.text,
  );

  static const visibility = Icon(
    Icons.visibility,
    color: AppColors.grey,
  );

  static const visibilityOff = Icon(
    Icons.visibility_off,
    color: AppColors.grey,
  );

  static const skip = Icon(
    Icons.swipe_right,
    color: AppColors.grey,
  );

  static const forward = Icon(
    Icons.arrow_forward_ios,
    color: AppColors.grey,
  );
}
